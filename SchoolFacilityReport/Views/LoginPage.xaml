<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:SchoolFacilityReport.ViewModels"
             xmlns:strings="clr-namespace:SchoolFacilityReport.Resources.Strings"
             x:Class="SchoolFacilityReport.Views.LoginPage"
             Title="{x:Static strings:AppResources.PageTitle_Login}"
             BackgroundColor="#FFFFFF">

    <ScrollView>
        <VerticalStackLayout Spacing="25" Padding="30" VerticalOptions="Center">

            <Button Text="🌐" 
                    Command="{Binding SwitchLanguageCommand}" 
                    HorizontalOptions="End"
                    BackgroundColor="Transparent" 
                    TextColor="#333"
                    FontSize="20"/>

            <Image x:Name="LogoImage"
                   Source="dotnet_bot.png" 
                   HeightRequest="120" 
                   HorizontalOptions="Center"
                   Margin="0,0,0,10"/>

            <VerticalStackLayout Spacing="0">
                <Label Text="{x:Static strings:AppResources.LoginTitle}" 
                       FontSize="28" 
                       FontAttributes="Bold" 
                       TextColor="Black"
                       HorizontalOptions="Center" 
                       HorizontalTextAlignment="Center"/>
                <Label Text="FACILITY MANAGEMENT v1.0" 
                       FontSize="12" 
                       TextColor="#888" 
                       CharacterSpacing="2"  HorizontalOptions="Center"
                       Margin="0,5,0,0"/>
            </VerticalStackLayout>

            <VerticalStackLayout Spacing="15" Margin="0,20,0,0">
                <Border Stroke="#E0E0E0" StrokeThickness="1" StrokeShape="RoundRectangle 8" Padding="15,2" BackgroundColor="#FAFAFA">
                    <Entry Placeholder="{x:Static strings:AppResources.EmailPlaceholder}" 
                           Text="{Binding Email}" 
                           Keyboard="Email"
                           TextColor="#333"
                           PlaceholderColor="#999"/>
                </Border>

                <Border Stroke="#E0E0E0" StrokeThickness="1" StrokeShape="RoundRectangle 8" Padding="15,2" BackgroundColor="#FAFAFA">
                    <Entry Placeholder="{x:Static strings:AppResources.PasswordPlaceholder}" 
                           Text="{Binding Password}" 
                           IsPassword="True"
                           TextColor="#333"
                           PlaceholderColor="#999"/>
                </Border>
            </VerticalStackLayout>

            <Border StrokeThickness="0" StrokeShape="RoundRectangle 25" HeightRequest="50" Margin="0,10,0,0">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#4A00E0" Offset="0.0" />
                        <GradientStop Color="#8E2DE2" Offset="1.0" />
                    </LinearGradientBrush>
                </Border.Background>
                <Border.Shadow>
                    <Shadow Brush="#8E2DE2" Offset="0,5" Radius="10" Opacity="0.4"/>
                </Border.Shadow>

                <Grid>
                    <Label Text="{x:Static strings:AppResources.LoginBtn}" 
                           TextColor="White" FontAttributes="Bold" FontSize="16" 
                           HorizontalOptions="Center" VerticalOptions="Center"/>
                    <Button BackgroundColor="Transparent" 
                            Command="{Binding SignInCommand}"/>
                </Grid>
            </Border>

            <Label HorizontalOptions="Center" TextColor="Gray" Margin="0,10,0,0">
                <Label.FormattedText>
                    <FormattedString>
                        <Span Text="No account? " />
                        <Span Text="{x:Static strings:AppResources.RegisterBtn}" 
                              TextColor="#4A00E0" 
                              FontAttributes="Bold"
                              TextDecorations="Underline">
                            <Span.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding SignUpCommand}"/>
                            </Span.GestureRecognizers>
                        </Span>
                    </FormattedString>
                </Label.FormattedText>
            </Label>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>